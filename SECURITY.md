# Security

Logos Inspector is a local diagnostic tool. Treat wallet files, private keys,
and endpoint credentials as secrets.

Do not paste private keys or mnemonic phrases into the GUI. The GUI should only
receive public account IDs, transaction hashes, program IDs, IDLs, and endpoint
URLs.

If you discover a security issue, report it through the repository's security
advisory process after the public repository is created.
