.import "LezTargetPresentation.js" as LezTargetPresentation

function applyResolvedTarget(root, response, errorTitle) {
    return LezTargetPresentation.applyResolvedTarget(root, response, errorTitle)
}
